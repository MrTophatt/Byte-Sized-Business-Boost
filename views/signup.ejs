<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Sign Up | Byte-Sized Business Boost</title>
        <link rel="icon" type="image/x-icon" href="/images/site/icon.svg">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="/signup/signup.css">
    </head>
    <body class="p-5">
        <div class="signup-box">
            <h2 class="mb-4">Create your account</h2>

            <form id="signupForm" class="auth-form">
                <input id="signupUsername" class="form-control mb-2" type="text" placeholder="Username" required>
                <input id="signupEmail" class="form-control mb-2" type="email" placeholder="Email" required>
                <input id="signupPassword" class="form-control mb-2" type="password" placeholder="Password (8+ characters)" minlength="8" required>
                <button class="btn btn-primary w-100" type="submit">Send Verification Code</button>
            </form>

            <a href="/login" class="btn btn-outline-dark w-100 mt-2">Back to Login</a>
            <p id="authMessage" class="auth-message"></p>
        </div>

        <div class="modal fade" id="verificationModal" tabindex="-1" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">Verify your email</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <p class="small text-muted">We sent a verification code to your email. Enter it below to complete signup.</p>
                        <input id="verificationCode" class="form-control" type="text" placeholder="6-digit code" maxlength="6">
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="button" class="btn btn-primary" id="confirmVerificationBtn">Verify & Create Account</button>
                    </div>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/signup/auth.js"></script>
    </body>
</html>